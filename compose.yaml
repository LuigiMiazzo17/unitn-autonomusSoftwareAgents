services:
  deliveroo:
    build:
      context: ./Deliveroo.js/
      dockerfile: Dockerfile
    environment:
      PORT: 8080
      ADMIN_PASSWORD: admin
      SUPER_SECRET: supersecretkey

    ports:
      - "8080:8080/tcp"
